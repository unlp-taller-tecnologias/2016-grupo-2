{% extends 'Admin/layout.html.twig' %}

{% block main %}
    <h1>Servicio</h1>

    <table class="table table-striped table-hover">
        <tbody>
           
            <tr>
                <th>Nombre del Servicio</th>
                <td>{{ servicio.tipo }}</td>
            </tr>
            <tr>
                <th>Descripción</th>
                <td>{{ servicio.descripcion }}</td>
            </tr>
            
        </tbody>
    </table>

    <table class="table table-striped">
        <tbody>
        <tr>
            <td>
                <a href="{{ path('admin_servicio_index') }}" class="btn btn-primary"> <i class="fa fa-arrow-left" aria-hidden="true"></i>
                    Volver al Listado </a>
            </td>
            <td>
                <a href="{{ path('admin_servicio_edit', { 'id': servicio.id }) }}" class="btn btn-warning"> <i class="fa fa-pencil" aria-hidden="true"></i>
                    Editar </a>
            </td>
            <td>
                {{ form_start(delete_form) }}
                <button type="submit" class="btn btn-danger"><i class="fa fa-trash" aria-hidden="true"></i>
                    Eliminar </button>
                {{ form_end(delete_form) }}
            </td>
        </tr>
        </tbody>
    </table>
{% endblock %}
