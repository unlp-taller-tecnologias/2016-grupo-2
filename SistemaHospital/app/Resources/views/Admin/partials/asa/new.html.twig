{% extends 'Admin/layout.html.twig' %}

{% block main %}
    <h1>Nueva ASAs</h1>

    {% if error is defined %}
        <div class="alert alert-danger">{{ error }}</div>
    {% endif %}

    {{ form_start(form) }}
    <div class="form-group">
        {{ form_widget(form) }}
    </div>
    <div class="form-group">
        <button type="submit" class="btn btn-success col-sm-offset-10"> <i class="fa fa-check" aria-hidden="true"></i>
            Confirmar </button>
    </div>
    {{ form_end(form) }}

    <table class="table table-striped">
        <tbody>
        <tr>
            <td>
                <a href="{{ path('admin_asa_index') }}" class="btn btn-primary"> <i class="fa fa-arrow-left" aria-hidden="true"></i>
                    Volver al Listado </a>
            </td>
        </tr>
        </tbody>
    </table>
{% endblock %}
