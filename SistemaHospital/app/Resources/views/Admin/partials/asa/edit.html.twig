{% extends 'Admin/layout.html.twig' %}

{% block main %}
    <h1>Modificar información del ASAs</h1>

    {{ form_start(edit_form) }}
    <div class="form-group">
        {{ form_widget(edit_form) }}
    </div>
    <div class="form-group">
        <button type="submit" class="btn btn-success col-sm-offset-10"> <i class="fa fa-check" aria-hidden="true"></i>
            Confirmar </button>
    </div>
    {{ form_end(edit_form) }}

    <table class="table table-striped">
        <tbody>
        <tr>
            <td>
                <a href="{{ path('admin_asa_show', { 'id': asa.id }) }}" class="btn btn-primary">
                    <i class="fa fa-arrow-left" aria-hidden="true"></i> Volver </a>
            </td>
        </tr>
        </tbody>
    </table>
{% endblock %}
